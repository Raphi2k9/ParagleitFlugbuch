package com.example.flugbuch.ui.theme

import androidx.compose.ui.graphics.Color

// ── Light Mode ─────────────────────────────────────────────────────────────
// Frisches Himmel-Blau als Primary (Indigo-Blue, modern & klar)
val LightPrimary            = Color(0xFF2563EB) // Blue-600
val LightOnPrimary          = Color(0xFFFFFFFF)
val LightPrimaryContainer   = Color(0xFFDBEAFE) // Blue-100
val LightOnPrimaryContainer = Color(0xFF1E3A8A) // Blue-900

// Cyan/Teal als Secondary (Himmel trifft Wasser)
val LightSecondary            = Color(0xFF0891B2) // Cyan-600
val LightOnSecondary          = Color(0xFFFFFFFF)
val LightSecondaryContainer   = Color(0xFFCFFAFE) // Cyan-100
val LightOnSecondaryContainer = Color(0xFF164E63) // Cyan-900

// Amber als Tertiary (Sonnenuntergang)
val LightTertiary            = Color(0xFFD97706) // Amber-600
val LightOnTertiary          = Color(0xFFFFFFFF)
val LightTertiaryContainer   = Color(0xFFFEF3C7) // Amber-100
val LightOnTertiaryContainer = Color(0xFF78350F) // Amber-900

// Hintergründe (reines Weiß + minimal blaue Oberflächen)
val LightBackground        = Color(0xFFFFFFFF)
val LightSurface           = Color(0xFFF8FAFF)
val LightSurfaceVariant    = Color(0xFFEFF4FF)
val LightOnBackground      = Color(0xFF0F172A) // Slate-900
val LightOnSurface         = Color(0xFF1E293B) // Slate-800
val LightOnSurfaceVariant  = Color(0xFF475569) // Slate-600
val LightOutline           = Color(0xFFCBD5E1) // Slate-300

// ── Dark Mode ───────────────────────────────────────────────────────────────
// Echtes Schwarz/Dunkelgrau – AMOLED-friendly, kein Blau-Stich
val DarkPrimary            = Color(0xFF60A5FA) // Blue-400 – leuchtet auf Schwarz
val DarkOnPrimary          = Color(0xFF1E3A8A) // Blue-900
val DarkPrimaryContainer   = Color(0xFF1D3461) // tiefes Marineblau
val DarkOnPrimaryContainer = Color(0xFF93C5FD) // Blue-300

val DarkSecondary            = Color(0xFF22D3EE) // Cyan-400
val DarkOnSecondary          = Color(0xFF164E63) // Cyan-900
val DarkSecondaryContainer   = Color(0xFF0C3547) // dunkles Cyan
val DarkOnSecondaryContainer = Color(0xFF67E8F9) // Cyan-300

val DarkTertiary            = Color(0xFFFBBF24) // Amber-400
val DarkOnTertiary          = Color(0xFF451A03)
val DarkTertiaryContainer   = Color(0xFF3D2200)
val DarkOnTertiaryContainer = Color(0xFFFDE68A)

// Hintergründe (echtes Schwarz → Anthrazit → Dunkelgrau)
val DarkBackground       = Color(0xFF0A0A0A) // fast Schwarz
val DarkSurface          = Color(0xFF141414) // Karten-Hintergrund
val DarkSurfaceVariant   = Color(0xFF1F1F1F) // leicht abgehobene Flächen
val DarkOnBackground     = Color(0xFFF1F5F9) // Slate-100
val DarkOnSurface        = Color(0xFFE2E8F0) // Slate-200
val DarkOnSurfaceVariant = Color(0xFF94A3B8) // Slate-400
val DarkOutline          = Color(0xFF334155) // Slate-700

// ── Semantic ────────────────────────────────────────────────────────────────
val ErrorRed          = Color(0xFFEF4444)
val ErrorRedContainer = Color(0xFF7F1D1D)
